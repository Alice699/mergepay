# Deployments

Network-specific program IDs, compatibility status, artifact sizes, hashes, and links
to reproducible evidence live here. A record must distinguish a fully tested deployed
program from a newer local artifact that is still awaiting deployment.

No private keys or environment credentials belong in deployment records.
