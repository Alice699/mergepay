# Web Tests

- `unit/` will cover components, validation, and feature state.
- `e2e/` will cover wallet-aware workflow journeys.
- `structure.test.mjs` protects the architectural boundaries before feature work begins.
