# PDA Derivation

Workflow PDA derivation preserves Rialo Venus' payer-plus-slug semantics. The auxiliary
subscription and REX addresses use the framework's `multi_account_slug` SHA-256 helper
and the exact generated account indexes. Every derivation is covered by fixtures from
the runtime-proven DevNet workflow.
