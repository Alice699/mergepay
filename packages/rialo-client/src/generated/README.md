# Generated Interface

`mergepay.ts` is the reviewed, checked-in TypeScript mirror of
`programs/mergepay-rialo/wit/mergepay-rialo-manifest.json`. It contains only protocol
constants: the active program ID, well-known interface addresses, instruction
discriminants, account indexes, seed labels, and state-size metadata.

If the manifest or deployed program changes, regenerate/review this file together with
the runtime evidence before changing frontend behavior.
